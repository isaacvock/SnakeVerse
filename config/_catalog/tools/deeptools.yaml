# Editable deepTools bamCoverage profile.
tool: deeptools
version: "3.5"

params:
  bin_size: 10
  normalize_using: CPM
  min_mapping_quality: 10
  ignore_duplicates: false
  extend_reads: false

# Appended verbatim to the bamCoverage command.
extra: ""

